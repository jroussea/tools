Bootstrap: docker
From: continuumio/anaconda3

%post
	apt-get update && apt install -y procps g++
	conda install -c conda-forge -c bioconda mcl==22.282

%labels
	Maintener Jérémy Rousseau, MNHN - Paris
	Version 1.0
